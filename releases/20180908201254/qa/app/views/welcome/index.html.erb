<div class="text-center">
  <h1><strong>Welcome to Mike's Q and A</strong></h1>
  <p><em>There are no stupid questions. There are many stupid answers.</em></p>
  <p id="notice"><%= notice %></p>

  <br>

  <h6>Since I don't know how logins work yet, just pick your user or make a new one.</h6>

  <br>

  <div>
    <%= link_to 'Create New User', new_user_path %>
  </div>

  <br>

  <% @users = User.all %>
  <% if @users.any? %>
  <table class="mx-auto">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Link</th>
      </tr>
    </thead>

    <tbody>

      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= link_to 'Select', user_questions_path(user) %>
        </tr>

      <% end %>
    </tbody>
  </table>
  <% else %>
  <p>Looks like you are our first user! Use the link below to create your profile</p>
  <% end %>

  <br>

  <%= link_to 'All Users', users_path %>
</div>

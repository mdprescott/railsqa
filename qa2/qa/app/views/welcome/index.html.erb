<div class="text-center">
  <h1><strong>Welcome to Mike's Q and A</strong></h1>
  <p><em>There are no stupid questions. Only stupid people.</em></p>
  <p id="notice"><%= notice %></p>
</div>
<br>
<h6 class="text-center">Since I don't know how to code logins yet, just pick your user or make a new one.</h6>
<br>
<% @users = User.all %>
<% if @users.any? %>
<table class="mx-auto text-center">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Select', user_questions_path(user) %>
        <td><%= link_to 'Show', user %></td>
        <td class="user-admin"><%= link_to 'Edit', edit_user_path(user) %></td>
        <td class="user-admin"><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>

    <% end %>
  </tbody>
</table>
<% else %>
<p class="text-center">Looks like you are our first user! Use the link below to create your profile</p>
<% end %>
<br>

<div class="text-center">
  <%= link_to 'New User', new_user_path %> |
  <%= link_to 'All Users', users_path %>
</div>
